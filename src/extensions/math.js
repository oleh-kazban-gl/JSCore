export const negative = -10;
export const neutral = 0;
export const positive = 10;

export const nullable = null;
export const undefinite = undefined;
export const nan = NaN;
export const infinite = Infinity;

export const positiveFloat = 10.123;
export const negativeFloat = -10.123;

// --- Math.sign() ---
// console.log('negative Math.sign', Math.sign(negative));
// console.log('neutral Math.sign', Math.sign(neutral));
// console.log('positive Math.sign', Math.sign(positive));

// console.log('nullable Math.sign', Math.sign(nullable));
// console.log('undefinite Math.sign', Math.sign(undefinite));
// console.log('nan Math.sign', Math.sign(nan));
// console.log('infinite Math.sign', Math.sign(infinite));

// --- Math.trunc() vs Math.floor() ---
// console.log('positiveFloat Math.trunc', Math.trunc(positiveFloat));
// console.log('negativeFloat Math.trunc', Math.trunc(negativeFloat));

// console.log('positiveFloat Math.floor', Math.floor(positiveFloat));
// console.log('negativeFloat Math.floor', Math.floor(negativeFloat));
